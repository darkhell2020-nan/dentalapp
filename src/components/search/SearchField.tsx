import { Search } from 'lucide-react';
export function SearchField({value,onChange,autoFocus=false,compact=false}:{value:string;onChange:(value:string)=>void;autoFocus?:boolean;compact?:boolean}){return <label className={`group flex items-center gap-3 rounded-[1.35rem] border border-[rgba(47,125,119,.24)] bg-white/95 px-4 shadow-[0_18px_55px_rgba(30,60,70,.11)] ${compact?'py-2':'py-4 md:py-5'}`}><Search className="text-[#2f7d77]" size={compact?20:24}/><input autoFocus={autoFocus} aria-label="Search topics" value={value} onChange={(e:{target:{value:string}})=>onChange(e.target.value)} placeholder="Search crown, denture, filling, implant..." className={`w-full border-0 bg-transparent outline-none placeholder:text-slate-400 ${compact?'text-base':'text-xl md:text-2xl'}`}/><kbd className="hidden rounded-lg border border-slate-200 bg-slate-50 px-2 py-1 text-xs font-semibold text-slate-400 md:block">/</kbd></label>}
