import type { ReactNode } from 'react';
import { Home, Heart, Info, Library } from 'lucide-react';import { Link } from 'react-router-dom';import type { LanguageMode } from '../../types/content';import { LanguageToggle } from './LanguageToggle';
export function AppChrome({language,onLanguageChange,children}:{language:LanguageMode;onLanguageChange:(value:LanguageMode)=>void;children:ReactNode}){const nav=[['/','Home',Home],['/#browse','Browse',Library],['/#favourites','Favourites',Heart],['/#info','Info',Info]] as const;return <div className="app-shell"><div className="container-premium py-4 md:py-6"><header className="glass-nav desktop-nav sticky top-4 z-20 mb-8 flex items-center justify-between rounded-full px-4 py-2"><Link to="/" className="flex items-center gap-2 px-2 font-black tracking-[-0.03em]"><span className="grid h-9 w-9 place-items-center rounded-full bg-[#2f7d77] text-white">D</span>DentVisual</Link><nav className="flex items-center gap-1">{nav.map(([to,label,Icon])=><Link key={label} to={to} className="btn btn-ghost text-sm"><Icon size={17}/>{label}</Link>)}</nav><LanguageToggle value={language} onChange={onLanguageChange}/></header>{children}</div><nav className="bottom-nav glass-nav items-center justify-around rounded-[1.4rem] p-2">{nav.slice(0,3).map(([to,label,Icon])=><Link key={label} to={to} className="grid place-items-center rounded-2xl px-3 py-1 text-xs font-bold text-slate-600"><Icon size={20}/>{label}</Link>)}</nav></div>}
