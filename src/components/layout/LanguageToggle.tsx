import type { LanguageMode } from '../../types/content';
export function LanguageToggle({value,onChange}:{value:LanguageMode;onChange:(v:LanguageMode)=>void}){return <div role="group" aria-label="Language" className="inline-flex rounded-full border border-slate-200 bg-white/80 p-1 shadow-sm">{(['en','bm','both'] as LanguageMode[]).map(l=><button key={l} className={`min-h-9 rounded-full px-3 text-sm font-bold transition ${value===l?'bg-[#172033] text-white shadow-sm':'text-slate-500 hover:text-[#172033]'}`} onClick={()=>onChange(l)}>{l==='en'?'EN':l==='bm'?'BM':'EN+BM'}</button>)}</div>}
export function LanguageToggle({value,onChange}:{value:LanguageMode;onChange:(v:LanguageMode)=>void}){return <div role="group" aria-label="Language" className="flex gap-1 rounded-full bg-slate-100 p-1">{(['en','bm','both'] as LanguageMode[]).map(l=><button key={l} className={`btn ${value===l?'btn-primary':''}`} onClick={()=>onChange(l)}>{l==='en'?'EN':l==='bm'?'BM':'EN+BM'}</button>)}</div>}
