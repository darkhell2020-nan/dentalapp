import { useRef,useState } from 'react';type Stroke={tool:string;pts:[number,number][]};
export function AnnotationCanvas({active}:{active:boolean}){const [strokes,setStrokes]=useState<Stroke[]>([]);const [tool,setTool]=useState('pen');const drawing=useRef<Stroke|null>(null);if(!active)return null;const pt=(e:React.PointerEvent<SVGSVGElement>):[number,number]=>{const r=e.currentTarget.getBoundingClientRect();return [e.clientX-r.left,e.clientY-r.top]};return <div className="absolute inset-0"><svg className="absolute inset-0 h-full w-full touch-none" onPointerDown={(e:React.PointerEvent<SVGSVGElement>)=>{drawing.current={tool,pts:[pt(e)]};(e.target as Element).setPointerCapture(e.pointerId)}} onPointerMove={(e:React.PointerEvent<SVGSVGElement>)=>{if(drawing.current){drawing.current.pts.push(pt(e));setStrokes([...strokes,drawing.current])}}} onPointerUp={()=>{if(drawing.current)setStrokes([...strokes,drawing.current]);drawing.current=null}}>{strokes.map((s,i)=><polyline key={i} points={s.pts.map(p=>p.join(',')).join(' ')} fill="none" stroke={s.tool==='pen'?'#dc2626':'#facc15'} strokeWidth={s.tool==='pen'?4:14} strokeOpacity={s.tool==='pen'?1:.45} strokeLinecap="round" strokeLinejoin="round"/>)}</svg><div className="absolute bottom-3 left-1/2 flex -translate-x-1/2 gap-2 rounded-full bg-white/95 p-2 shadow"><button className="btn" onClick={()=>setTool('pen')}>Pen</button><button className="btn" onClick={()=>setTool('highlighter')}>Highlighter</button><button className="btn" onClick={()=>setStrokes(strokes.slice(0,-1))}>Undo</button><button className="btn" onClick={()=>setStrokes([])}>Clear</button></div></div>}
